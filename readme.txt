git clone fileName


3 stages => add, commit, push

git add .

git commit -m"Commit message"

git push origin branchName

Configuration => 

git config --global user.name ""
git config --global user.email ""

clone 

make changes 

add, commit, push

ls => list files

cd => change directory/folder

cd .. => go to parent folder/directory


1) Configuration => 

one time 
git config --global user.name "githubUsername"
git config --global user.email "githubEmail"

2) Create repo in github -> one project => flipkart clone

3) Clone github repo to local system
git clone fileName

4) make changes/add files/complete ur project

5) add, commit, push

login => ui
add,commit, push

functionality
add,commit, push


whatsapp clone => Create a repo in github
